/* ############################################################## 
######################### AULA 04 ###############################
################################################################# */


$(document).ready(()=>{

    /* // Selecionando o último elemento.
    $('.cars_list li:last').click( ()=>{
        alert('Ok')
    } )
    */

    /* // Selecionando o primeiro elemento.
    $('.cars_list li:first').click( ()=>{
        alert('Cliquei no primeiro Item')
    } )
    */

    // Selecionando elementos de maneira alternada por pares.
    //$('.cars_list li:even').css('background-color','grey');

    // Selecionando elementos de maneira alternada por impares.
    //$('.cars_list li:odd').css('background-color','grey');

    // Selecionando um elemento especifico.
    //$('.cars_list li:eq(1)').css('font-size','40px');

    // Selecionando todos elementos abaixo de outro.
    //$('.cars_list li:gt(2)').css('opacity','0.3');

    // Selecionando todos elementos acima de outro.
    //$('.cars_list li:lt(2)').css('opacity','0.3');

});

