$(document).ready(()=>{
    // Retorna ou define uma propriedade.
    //$("form [type=checkbox]").prop('checked',true);

    /* // Armazena propiedade de forma virtual fora do html.
    const armazena = (string) => { // armazena em prop nome
            $("h1").prop("nome",string);
            console.log($("h1").prop("nome"));
    }

    const consulta = () => { // consulta em prop.
        console.log($("h1").prop("nome"));
    }

        // Execução.
        armazena("titulo"); // retorna titulo.
        consulta(); // retorna o que tem na prop nome.
        armazena("teste"); // retorna vazio.
        consulta(); // retorna o que tem na prop nome
    */

    // Remover propriedade.
    //$("h1").prop("nome","Titulo");
    //console.log();

}); 