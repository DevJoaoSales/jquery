$(document).ready(()=>{

    /* // Retornando ou definindo a posição de um elemento, se baseando no tamanho total da pagina.
    $("form .field:eq(1)").offset({
        top:100,
        left:100,
    }); */

    /* // Apenas retorna posição do elemento, se baseando no tamanho total do elemento pai.
    let teste = $("form .field:eq(1)").position();
    console.log(teste);
    */
    
    // Retorna ou definir posição da barra de rolagem Vertical.
    //$(document).scrollTop(200);

    // Retorna ou definir posição da barra de rolagem Horizontal.
    //$(document).scrollLeft(200);

});