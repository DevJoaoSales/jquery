$(document).ready(()=>{

    // Retornando ou alterando o Conteudo HTML de um elemento.
    //$("header .menu li:eq(1)").html();
    //$("header .menu li:eq(1)").html("<a href='https://www.hcode.com.br'>Home</a>");

    // Retornando ou alterando texto do elemento.
    //$("header .menu li:eq(2)").text();
    //$("header .menu li:eq(2) a").text("Teste")

    // Redefinindo o conteudo de um elemento.
    //$("form button").replaceWith("<h4 style='color:red;text-align:center;'>Registre-se</h4>");

    // Redefinindo o conteudo de um elemento com outra sintaxe.
    //$("<h4 style='color:red;text-align:center;'>Registre-se</h4>").replaceAll("form button");
});