$(document).ready(()=>{

    // Retorna ou define a largura (width) do elemento sem considerar o padding.
    //$('.paragrafo').width(200);

    // Retorna ou define a largura (width) do elemento considerando o padding.
    //$('.paragrafo').innerWidth(200);

    // Retorna ou define a altura (height) do elemento sem considerar o padding.
    //$('.paragrafo').height(200);

    // Retorna ou define a altura (height) do elemento considerando o padding.
    //$('.paragrafo').innerHeight(200);


});